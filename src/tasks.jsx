/*  tasks.jsx
    Manages tasks and everything associated with them
    For the game Settlers & Warlords
*/

// We will provide a separate task object, that includes functions to manage itself

// Buildings will still have general task data, and determine if a given task is able to be assigned

export function createTask() {
    // This only generates top-level tasks. Additional tasks can be generated from this, including subtasks or side-tasks (which can be completed
    // by another worker)... but we'll worry about that when we get there
    
    // Tasks need to be set in a way that can be saved and loaded seamlessly.

    
}